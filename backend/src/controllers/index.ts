import { getUserController, postUserController, patchUserController, deleteUserController } from "./users.controllers";
import { getContactsController, deleteContactController, patchContactController, postContactController } from "./contacts.controller";
import { loginController } from "./login.controller";

export {
    getUserController,
    postUserController,
    patchUserController,
    deleteUserController,
    getContactsController,
    deleteContactController,
    patchContactController,
    postContactController, 
    loginController,
}