import { postUserSchema, patchUserSchema } from "./users.schemas";
import { postContactSchema, patchContactSchema } from "./contacts.schema";
import { loginSchema } from "./login.schema";

export {
    postUserSchema,
    patchUserSchema,
    postContactSchema,
    patchContactSchema,
    loginSchema,
}